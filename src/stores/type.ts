export interface Act {
    id: string | null
    name: string
    cost: number
    tag: string
}

export interface Rec {
    id: string
    name: string
}

export interface ResRec {
    id: string
    num: number
}